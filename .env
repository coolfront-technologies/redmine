# Gitignored — used by `docker compose` (env_file + variable substitution).

# Single URL for Postgres (Rails `/start.sh` uses ENV["DATABASE_URL"] in generated config/database.yml).
# Local Compose + `db` service — switch to:
#   DATABASE_URL=postgres://postgres:password@db:5432/myapp_development

DATABASE_URL=postgres://cfadmin:Y2yjcpT2N8Agrn1BNFtB@coolfront-southcentral-prod-common-psql.postgres.database.azure.com:5432/de7fbed2p60aqe?sslmode=require

SKIP_S3_BOOTSTRAP=1
REDMINE_NO_DB_MIGRATE=1
